apiVersion: apps/v1
kind: Deployment
metadata:
  name: jira-dashboard
  namespace: jira-auto
  labels:
    app: jira-dashboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: jira-dashboard
  template:
    metadata:
      labels:
        app: jira-dashboard
    spec:
      containers:
        - name: app
          image: inikolov98/my-jira-automation:dev
          ports:
            - containerPort: 8000
          env:
            - name: APP_NAME
              value: "jira-automation-dashboard"
            - name: ENVIRONMENT
              value: "dca147cf6a5ac939607f1824f7a4765228353a12"
            - name: APP_VERSION
              value: "dca147cf6a5ac939607f1824f7a4765228353a12"
          envFrom:
            - secretRef:
                name: jira-cloud-credentials

          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20
